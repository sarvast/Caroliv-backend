# Caloriv Backend Environment Variables
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=development

# Security
# IMPORTANT: Generate a strong secret key (at least 32 characters)
# You can use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-change-this-in-production

# Razorpay Payment Gateway
# Get these from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Google Gemini AI (Optional)
# Get from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key

# CORS Configuration (Optional)
# Comma-separated list of allowed origins for production
# Example: https://caloriv-admin.vercel.app,https://caloriv-web.vercel.app
CORS_ORIGIN=

# Email Configuration (Optional - for future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Redis Configuration (Optional - for caching)
# REDIS_URL=redis://localhost:6379

# Sentry Error Tracking (Optional)
# SENTRY_DSN=your_sentry_dsn
